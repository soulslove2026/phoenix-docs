# Real-Browser Passkey Validation

Use the staging-only same-origin harness to collect sanitized evidence across supported browsers, operating systems, platform authenticators, synced passkeys, and external security keys. Raw session tokens and credential responses must never enter evidence files.
