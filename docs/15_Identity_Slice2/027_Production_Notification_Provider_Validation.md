# Production Notification Provider Validation

Run the explicit provider smoke command only with a controlled mailbox, approved credentials, and SEND_TEST_NOTIFICATION confirmation. Record delivery, latency, bounce handling, provider logs, and alert behavior without storing secrets in GitHub.
