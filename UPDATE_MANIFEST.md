# Update Manifest — v3.0.0

Adds CORE-001 through CORE-009 and a complete standalone `phoenix-core` Slice 0 repository. Updates all root indexes, release metadata, manifest, checksums, and integrity report.
